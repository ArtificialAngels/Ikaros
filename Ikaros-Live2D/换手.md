# 换手记录 — Ikaros-Live2D 悬浮球/托盘/监控/追踪

## 已提交 (commit bea4689)

- **FloatBall 拖拽** — Tauri native `start_dragging()`, 无 IPC 延迟, 双击还原
- **托盘动态 checkmark** — `TrayMenuState` + `sync_tray_menu` 命令 + `build_menu()` 动态生成 `✓ ` 前缀
- **监控面板独立窗口** — `monitor.html` + `monitor-main.ts`, 通过 Tauri events 通信
  - 主窗口发射 `monitor-status` / `monitor-event`
  - 独立窗口通过 `listen()` 接收显示
- **穿透/交互模式切换** — `manualInteractive` ref + 轮询跳过 + corner override 防闪烁
- **输入轮询重构** — `poll_input_state` 单命令/20ms, click-through 管理在 Rust 侧闭环
- **眼球追踪** — 全局追踪（窗口外跟随）, 50fps
- **窗口 bounds 缓存** — 主线程 `on_window_event(Moved|Resized)` 同步, 后台线程读缓存
- **置顶开关** + 缺失托盘功能补回

## 待验证

1. **监控独立窗口** — 代码就绪但未实测打开
   - 托盘右键「📊 监控面板」→ 通过 Rust `toggle_monitor_window` 开/关
   - 主窗口 📊 按钮 → 呼出嵌入式面板（旧逻辑）
2. **眼球追踪** — 已改为全局追踪（窗口外也跟随），若觉得过于敏感可调整 `150.0` 分子
   - 位置: `input_poll.rs` 第 146~155 行
3. **穿透模式回归** — `forceSetClickThrough(true)` → 设 `cornerOverride=true` 防角落自动弹回，若仍有异常需排查
4. **IPC 频率** — 20ms polling, 若觉得影响性能可在 `useMouseTracking.ts` 改回 30~50ms

## 关键文件位置

| 功能 | 文件 |
|------|------|
| 轮询命令 | `src-tauri/src/input_poll.rs` — `poll_input_state` |
| 窗口缓存同步 | `src-tauri/src/lib.rs` — `on_window_event` |
| JS 轮询入口 | `src/composables/useMouseTracking.ts` |
| 监控窗口入口 | `src/monitor-main.ts` + `monitor.html` |
| 托盘菜单 | `src-tauri/src/tray.rs` |
| 悬浮球 | `src/components/FloatBall.vue` |
| 穿透切换 | `src/App.vue` — `toggleMode()` |
