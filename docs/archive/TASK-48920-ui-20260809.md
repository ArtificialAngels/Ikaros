# TASK — 48920 对话树面板 UI 功能补齐（2026-08-09 交接 WorkBuddy）

> 工作目录：`E:\Ikaros`。主文件：`core/conversation-tree/index.html`（~5200 行，单文件前端）。
> 后端：`core/conversation-tree/server.py`。测试：`python -m pytest tests/`（基线 **82 passed**）。
> 自测服务：`python core/conversation-tree/server.py --port 48920` → http://127.0.0.1:48920

## 当前架构（已完成，勿回退）

- L1/L2/L3 = 同一会话节点卡片的三个形态（节点态/浏览态/激活态），所有展开卡都是独立完整容器
- 多 L3 标准容器：`miniUpgradeToL3(id)`（L2↔L3 切换，每卡挂完整四按钮输入框，`miniSend` 绑定各自节点走 chatStream 流式）
- 焦点切换：`setFocusCard(id)`（点击任意卡上横条 → 该卡升主卡，等级保持；原主卡降 mini 等级保持）
- 统一体积碰撞：`collidePushCards(dragId)` 收集全部 .node-card（L1/L2/L3），焦点卡锚定（质量无穷）；`reflowAroundCard(id, level)` 双锚（聚焦卡+焦点卡）
- mini 卡与主卡同配置：背景 var(--bg-card)、96px 五段渐变+40px 底、上横条 z-40 transparent、字号 100%、padding 0 2%；mini L3 内容 zoom:0.8（125% 尺寸补偿）
- 标题字号 JS 反算视觉恒定：applyTransform 内 `12/(0.8×zoom)`（L3）或 `12/zoom`（L2/mini）
- 输入框（主卡 #inputMain）：圆形 AI 模型按钮（Playfair 加粗）、发送按钮实心主题色+图标 bg-card、联网按钮左移对称（启用=反色无发光）、3 行及以上切换布局（滞回 64/48 + 96% 宽居中 + 黄金间距 gap=(宽-136)/4 左距=0.5gap=右距 + 过渡动画）、呼吸阴影 input-breathe 4s、跑马灯 conic 蓝粉（logo 色 #8ec5e8/#f0a0b0，scheduleMarquee 随机 18-40s 触发 6s）
- L3 对话列表下极限：`updateThreadPad()` = 96 + 输入框高 + 16（renderThread 尾部/autoResize/setCardLevel/renderAll 同步）
- 关键函数：openNodeCard / openMiniCard / miniUpgradeToL3 / miniSend / setFocusCard / headerDragStart2 / collidePushCards / reflowAroundCard / cardSizeOf / autoResize / updateThreadPad / scheduleMarquee / bindMiniScrollDrag（mini 对话区拖动+惯性 0.93）

## 缺失功能（按优先级，全部要完成）

### 1. mini L3 输入框附件上传（当前 data-act="attach" 只提示"暂由主卡统一"）
- 每卡独立附件：动态创建本卡 `<input type="file">`（唯一 id），点击附件按钮触发
- 选图后显示附件 chips（参考主卡 renderAttachChips / _pendingAttaches 机制，chips 在输入框上方）
- 发送时附件以图片 markdown / 文件链接拼入消息（miniSend 已拼联网标记，同样拼附件）
- 附件列表存在 mini 元素上（如 `mini._files`），发送后清空

### 2. mini 输入框 3 行布局切换（与主卡 .tall 一致）
- 主卡有：autoResize 检测 scrollHeight>64 进入 tall（滞回退出 <48）、按钮行下移到输入框下方、4 按钮均匀分布（间距一致，左距=0.5×间距=右距，动态 gap=(行宽-136)/4）、文本框 96% 宽居中、过渡动画
- mini 输入框目前只有自动增高（max 88px）——需同样布局切换：mini-input 的按钮组（mini-input-left/right）在 3 行时下移到 textarea 下方均匀分布（4 按钮横向 space-evenly 或两组分布），输入区 96% 宽居中，过渡动画
- 复用主卡的滞回逻辑思路（防抖）

### 3. 视口边界约束（多卡自动布局全部可见）
- 现状：多 L3 卡时碰撞推挤可能把卡推出视口（画布可 pan/拖动找回，但用户要求打开即见）
- 要求：打开/升级卡时，reflowAroundCard 推挤结果尽量保持在视口内（clamp 到视口边缘内，含 margin）；或打开时自动 pan/zoom 使所有展开卡可见（焦点卡保持居中优先）
- 注意：nodePositions 是画布坐标（中心点），视口换算用 panX/panY/zoom（参考 screenToCanvas）

### 4. 主卡与 mini 输入框彻底统一（消灭"主卡专属"分支）
- #inputMain 的 JS（sendMessage/toggleWebSearch/openModelDropdown/附件/3行布局）与 mini 输入框（miniSend/mini-web/简化布局）存在双轨
- 要求：输入框逻辑组件化——同一套渲染/行为函数，按 nodeId 绑定；主卡与 mini 卡输入框同规格同行为（视觉差异仅允许：主卡 L3 是焦点态，尺寸略大）
- 可渐进：先抽公共函数（渲染模板、发送链路、附件链、布局切换），主卡和 mini 都调用

### 5. AI 回答气泡光晕在 mini 卡生效
- 主卡 .answer-live 有 ai-glow 动画（2.4s 蓝粉呼吸）——验证 mini 卡内 .ai-card.answer-live（miniSend 乐观气泡）也带动画；若无，补选择器

### 6. mini L3 输入框与对话列表联动（最后气泡不被输入框遮挡）
- 主卡有 updateThreadPad（thread-space 避让）——mini L3 卡同样需要：mini-body 底部 padding 随输入框高度动态增加（输入框顶到卡底距离+余量），保证最后一条消息可滚到输入框上方
- 在 mini 输入框 input 事件/3行切换时同步更新

## 验收标准（WorkBuddy 必须自我验收，逐项执行并记录）

1. **附件**：mini L3 点附件按钮 → 文件选择器 → 选图 → chips 显示 → 发送后消息含图片 markdown（可用 playwright 或手动）
2. **3 行布局**：mini L3 输入满 3 行 → 按钮下移均匀分布（实测间距相等、左距=0.5×间距=右距）→ 清空恢复原位
3. **视口**：开 3 张 L3 卡 → 全部卡在视口内可见（getBoundingClientRect 与 viewport 对比）→ 拖动/缩放正常
4. **统一**：主卡与 mini 输入框行为一致（附件/联网/发送/3行布局都有）
5. **回归**：`python -m pytest tests/` 全部通过（基线 82，不得减少）
6. **回归**：主卡 L3 输入框 3行布局/呼吸/跑马灯/焦点切换/mini 拖动惯性 不回归
7. **语法**：index.html 内 script 块全部 node --check 通过
8. **截图**：每项验收截图存 `E:\Ikaros\output\wb-*.png`，最终验收报告写 `E:\Ikaros\output\wb-report.md`（含每项实测数据）

## 注意事项

- index.html 是单文件（HTML+CSS+JS），改动前通读关键函数（见架构清单）
- 不要动 server.py 的缓存逻辑（no-cache 已验证，浏览器需 Ctrl+F5）
- 不要删 _cards Map / miniUpgradeToL3 / setFocusCard / collidePushCards 等既有接口（其他逻辑依赖）
- 共享变量污染是历史 bug 根源（_lastClick 曾与画布检测互踩）——新逻辑用独立变量
- 改完立即：提取 script 块 node --check + pytest
- 自测诚实记录：无法自动验证的项明确说明人工验证步骤
